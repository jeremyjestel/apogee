
.h is declaration for public use
.ccp is the private implementation each object has access to

.h and .cpp for every class
.h only for structs just need to be declared

build step
cmake -S . -B build
cmake --build build

creates apogee.pyd and must be copied next to main.py


Really not sure why the .inc cant be seen
