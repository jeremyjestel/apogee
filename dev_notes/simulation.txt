Fixed Timestep size that steps through

Scenario agnostic core


ECS Inspired

Data oriented design


Build wide breadth low depth sim of the bat

Need to decide on preprocess or live update

Debug tips:
Plot each variable to see if expected
Comment each section

Going to sim everything as 3 degrees of freedom earth agnostic in a global ECI.
If need Earth rotation, can convert to ECEF with time 
Anything that needs can be converted to local ENU with diff frames of reference
V1 will use the blue sattelite as the reference frame of 0,0,0.

Will be able to set the starting position, velocity, and acceleration of each object in the scene
Measure distances in m, m/s, m/s^2

Goal is to place into the scene and each entity has a team

I want to have objects with static analysis of each
Simulation for the interaction of each in time

Analysis will be done with plots in matplotlib
Simulation motion will be rerun and other data will be matplotlib

core/
    World: owns entities and associates IDs with components
components/
    Definitions of KinematicState, RadarComponent, etc.
archetypes/
    Functions that create an ID and assign a standard set of components
systems/
    Processes entities possessing particular components
math/
    Reusable types such as Vec3